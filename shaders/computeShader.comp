#version 450

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

layout(binding = 0, rgba8) writeonly uniform image2D destImage;

void main() {
ivec2 coord = ivec2(gl_GlobalInvocationID.xy);
imageStore(destImage, coord, vec4(0.0, 0.0, 1.0, 1.0));
}

